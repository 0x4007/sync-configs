{
  "watch": ["src/**/*", "tests/**/*"],
  "ext": "ts,json",
  "exec": "bun run dev-start",
  "ignore": ["node_modules/*"],
  "delay": 5000,
  "events": {
    "exit": "rm -f src/fixtures/ubiquity/.ubiquity-os/.git/index.lock",
    "crash": "rm -f src/fixtures/ubiquity/.ubiquity-os/.git/index.lock"
  }
}
